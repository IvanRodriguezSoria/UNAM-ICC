/*
 * README
 *
 * Autor: Ivan Rodriguez Soria
 * Numero de cuenta: 310215833
 * Correo electronico: vendeta.ivan@hotmail.com
 *
 * Practica 2 UNAM ICC:
 * Contiene la clase Matriz2x2 que pertmite construir matrices. Se
 * añadieron todas las operaciones de practica 1 y 2 mas: inversa()
 * y potencia().
 * Ademas se modifico toString().
 * Contiene la clase ListaCadena que permite construir una estructura
 * de datos llamada "lista" y se pueden realizar sus operaciones
 * correspondientes.
 *
 * Compilacion:
 * Se necesita tener instalado Apache Ant, JUnit y Java 8 SDK.
 * Dentro de la carpeta "practica2" ejecuta como andministrador
 * en la terminal el comando "user$ sudo ant" para compilar; seguido
 * de "user$ sudo ant test" para realizar las pruebas unitarias.
 *
 *
 * Ejecucion:
 * Dentro de la carpeta "practica1" ejecuta como administrador en
 * la terminal, el comando "user$ sudo java -jar practica2.jar".
 *
 *
 * Opcional:
 * Dentro de la carpeta "practica1" ejecuta como administrador en
 * la terminal, el comando "user$ sudo ant doc" para generar la
 * documentacion del proyecto.
 *
 */
